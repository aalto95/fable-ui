{
  "name": "sdui-renderer-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @sdui/renderer build && pnpm --filter sdui-renderer-web build",
    "dev": "pnpm --filter sdui-renderer-web dev",
    "format": "biome format --write",
    "lint": "biome check",
    "preview": "pnpm --filter sdui-renderer-web preview"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8"
  }
}
