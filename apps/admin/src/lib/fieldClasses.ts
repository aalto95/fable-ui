/** Tailwind class strings shared by the property panel and related forms */
export const formStackClass = "flex flex-col gap-2.5";

export const fieldBlockClass = "flex flex-col gap-1 text-sm";

export const fieldLabelClass = "text-xs font-semibold text-foreground";

export const inputClass =
  "w-full rounded-md border border-input bg-background px-2.5 py-2 text-sm text-foreground shadow-sm transition-colors focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 focus-visible:outline-none disabled:cursor-not-allowed disabled:opacity-50";

export const textareaClass =
  "min-h-20 w-full resize-y rounded-md border border-input bg-background px-2.5 py-2 font-mono text-xs leading-relaxed text-foreground shadow-sm transition-colors focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 focus-visible:outline-none disabled:cursor-not-allowed disabled:opacity-50";

export const checkRowClass = "flex items-center gap-2 text-sm";

export const emptyStateClass = "m-0 text-sm text-muted-foreground";
