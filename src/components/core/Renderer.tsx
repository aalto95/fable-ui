import type React from "react";
import { Button } from "../ui/button";

export const Renderer: React.FC = () => {
  return (
    <>
      <Button>Hello</Button>
      <Button>Hello</Button>
    </>
  );
};
